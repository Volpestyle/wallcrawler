# WallCrawler Demo Environment Variables
# Copy this file to .env and fill in your values

# OpenAI Configuration (for OpenAI models)
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o

# Anthropic Configuration (for Claude models)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Gemini Configuration (for Google Gemini models)
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-1.5-pro

# Ollama Configuration (for local models)
# Make sure Ollama is running on your machine first!
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3

# Google Cloud Configuration (for real-time Gemini pricing via Cloud Billing API)
# Optional: If not provided, no pricing data will be shown
GOOGLE_CLOUD_PROJECT_ID=your-gcp-project-id
GOOGLE_CLOUD_SERVICE_ACCOUNT_KEY=google_service_account_key.json

# Optional WallCrawler Settings
WALLCRAWLER_DEBUG=false
WALLCRAWLER_CACHE_DIR=.wallcrawler/cache

# Note: This demo uses the LOCAL provider for browser automation
# No cloud browser services are required

# Google Cloud Pricing API Setup (Optional):
# 1. Enable the Cloud Billing API in your GCP project
# 2. Create a service account with "Billing Account Viewer" role
# 3. Download the service account key as JSON file
# 4. Place the JSON file in this directory (e.g., google_service_account_key.json)
# 5. Set GOOGLE_CLOUD_SERVICE_ACCOUNT_KEY to the filename
# 6. Set GOOGLE_CLOUD_PROJECT_ID to your project ID
# If not configured, no pricing data will be shown (honest approach)